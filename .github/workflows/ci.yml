name: CI

on:
  push:
    branches:
      - main
      - master
      - develop
      - 'chore/**'
  pull_request:
    branches:
      - main
      - master
      - develop

jobs:
  test:
    name: Test on ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Setup Python (for native modules)
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies (Linux)
        if: runner.os == 'Linux'
        run: |
          npm install --global node-gyp @electron/rebuild
          npm ci --no-audit --no-fund
          npx electron-rebuild

      - name: Install dependencies (macOS)
        if: runner.os == 'macOS'
        run: |
          npm install --global node-gyp @electron/rebuild
          npm ci --no-audit --no-fund
          npx electron-rebuild

      - name: Install dependencies (Windows)
        if: runner.os == 'Windows'
        run: |
          npm install --global node-gyp @electron/rebuild
          npm ci --no-audit --no-fund
          npx electron-rebuild

      - name: Run unit tests
        run: npm run test:unit
        env:
          CI: true

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results-${{ matrix.os }}
          path: |
            coverage/
            test-results/
          retention-days: 7

  lint:
    name: Lint and format check
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install dependencies
        run: npm ci --no-audit --no-fund

      - name: Check for linting errors
        run: |
          echo "Linting check (placeholder - add eslint if needed)"
          # npm run lint

  # Optional: E2E tests (commented out by default as they are slow)
  # e2e:
  #   name: E2E tests
  #   runs-on: ubuntu-latest
  #   
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v4
  #
  #     - name: Setup Node.js
  #       uses: actions/setup-node@v4
  #       with:
  #         node-version: 20
  #         cache: 'npm'
  #
  #     - name: Install dependencies
  #       run: |
  #         npm ci --no-audit --no-fund
  #         npx playwright install --with-deps
  #
  #     - name: Run E2E tests
  #       run: npm run test:e2e
  #       env:
  #         CI: true
  #
  #     - name: Upload E2E artifacts
  #       if: always()
  #       uses: actions/upload-artifact@v4
  #       with:
  #         name: e2e-results
  #         path: |
  #           playwright-report/
  #           test-results/
  #         retention-days: 7

